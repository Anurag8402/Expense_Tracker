# 💰 Expense Tracker – Java Servlet, JSP & Hibernate Project

## 📌 Project Overview

The **Expense Tracker Application** is a **Java-based web application** developed using **Servlets, JSP, and Hibernate ORM**. The application allows users to register, manage their profiles, and track daily expenses. It follows the traditional **MVC architecture** where JSP acts as the View, Servlets as Controllers, and Hibernate handles database persistence.

This project is **Servlet and jsp project **. It is a **core Java web project**, fresher-level Java developer**.

---

## 🚀 Features

* User registration and login
* Add new expense records
* View expense history
* Update existing expenses
* Delete expenses
* User-wise expense tracking
* Database interaction using Hibernate ORM
* JSP-based UI

---

## 🛠️ Technology Stack

* **Java (Core Java)**
* **Servlets**
* **JSP**
* **Hibernate ORM**
* **MySQL**
* **HTML / CSS**
* **Apache Tomcat**
* **Maven** (if used)
* **Git & GitHub**

---

## 🗄️ Database Design

### Database Name

```
expense_tracker_db
```

---

### 1️⃣ Table: `hibernate_sequence`

This table is auto-generated by Hibernate to maintain primary key values.

| Column Name | Data Type |
| ----------- | --------- |
| next_val    | BIGINT    |

---

### 2️⃣ Table: `user_detail`

Stores user account information.

| Column Name | Data Type    | Description        |
| ----------- | ------------ | ------------------ |
| id          | INT (PK)     | User ID            |
| fullName    | VARCHAR(255) | User full name     |
| email       | VARCHAR(255) | User email address |
| password    | VARCHAR(255) | User password      |
| about       | VARCHAR(255) | About user         |

---

### 3️⃣ Table: `expense`

Stores expense records linked to users.

| Column Name | Data Type    | Description         |
| ----------- | ------------ | ------------------- |
| id          | INT (PK, AI) | Expense ID          |
| title       | VARCHAR(255) | Expense title       |
| description | VARCHAR(255) | Expense description |
| price       | VARCHAR(255) | Expense amount      |
| date        | VARCHAR(255) | Expense date        |
| time        | VARCHAR(255) | Expense time        |
| user_id     | INT (FK)     | User ID reference   |

---

## 🔗 Entity Relationship

* One **User** can have **multiple Expenses**
* Relationship managed using `user_id` as foreign key
* Implemented using Hibernate mappings

---

## 🧩 Application Flow (MVC)

1. **JSP** collects user input (View)
2. **Servlet** processes request (Controller)
3. **Hibernate DAO** performs DB operations (Model)
4. Response forwarded back to JSP

---

## ▶️ How to Run the Project

1. Clone the repository:

```bash
git clone https://github.com/<your-username>/expense-tracker.git
```

2. Import project into **Eclipse / IntelliJ** as a **Dynamic Web Project** or **Maven Project**

3. Configure MySQL database:

```sql
CREATE DATABASE expense_tracker_db;
```

4. Update Hibernate configuration (`hibernate.cfg.xml`):

```xml
<property name="hibernate.connection.url">jdbc:mysql://localhost:3306/expense_tracker_db</property>
<property name="hibernate.connection.username">root</property>
<property name="hibernate.connection.password">your_password</property>
```

5. Deploy project on **Apache Tomcat Server**

6. Access application in browser:

```
http://localhost:8080/ExpenseTracker/
```

---

## 🧪 Testing

* Manual testing through browser
* Database verification using MySQL Workbench
* CRUD operations validated

---

## 📌 Future Enhancements

* Password encryption
* Input validation
* Pagination for expenses
* Session management improvements
* Migration to Spring MVC / Spring Boot

---

## 👨‍💻 Author

**Anurag Yadav**
MCA | Java Developer
GitHub: [https://github.com/Anurag8402](https://github.com/Anurag8402)

---

## ⭐ Recruiter Note

This project demonstrates:

* Core Java web development skills
* Practical use of Servlets and JSP
* Hibernate ORM and database relationships
* MVC architecture understanding
